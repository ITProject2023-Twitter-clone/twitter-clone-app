import React from "react";

const CreateTweetButton = () => {
  return <button className="w-64 h-20 border border-primary rounded-full text-xl font-bold duration-300 hover:bg-primary">Tweet</button>;
};

export default CreateTweetButton;
