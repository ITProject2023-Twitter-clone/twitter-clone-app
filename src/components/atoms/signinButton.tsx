import React from "react";

const SigninButton = () => {
  return <button className="w-96 h-20 border rounded-full bg-transparent hover:bg-primary hover:text-base-200 font-bold hover:border-none">Sign in with GitHub !!!!!</button>;
};

export default SigninButton;
