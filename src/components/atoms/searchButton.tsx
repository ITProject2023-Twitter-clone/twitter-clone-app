import React from "react";

const SearchButton = () => {
  return (
    <button className="btn-outline btn w-40 rounded-full  text-base capitalize hover:border-none hover:bg-primary">
      Search
    </button>
  );
};

export default SearchButton;
