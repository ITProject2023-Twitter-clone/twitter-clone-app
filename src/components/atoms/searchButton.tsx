import React from "react";

const SearchButton = () => {
  return (
    <button className="btn-outline btn w-40 rounded-full border-[#B5CDF480] text-base capitalize hover:border-none hover:bg-primary">
      Search
    </button>
  );
};

export default SearchButton;
